# UriPack: urikv-log

Thin Markpact — capabilities from [`urikv/urikv/manifest.yaml`](urikv/urikv/manifest.yaml).
Handlers: existing ``python://`` refs (code in `urikv/`, not duplicated here).

Generated by `urisys/scripts/generate_pack_markpacts.py`.

```yaml markpact:pack
apiVersion: urisys.io/v1
kind: UriPack
metadata:
  id: urikv-log
  version: 0.1.0
  language: python
description: log:// capability pack.
schemes:
- log
capabilities:
- id: log-health
  uri: log://runtime/query/health
  kind: query
  operation: log.health
  handler: python://urikv.log_handlers:log_health
  side_effects: false
  approval: not_required
- id: log-streams
  uri: log://runtime/query/streams
  kind: query
  operation: log.streams
  handler: python://urikv.log_handlers:log_streams
  side_effects: false
  approval: not_required
- id: log-read
  uri: log://{stream}/query/read
  kind: query
  operation: log.read
  handler: python://urikv.log_handlers:log_read
  side_effects: false
  approval: not_required
- id: log-summarize
  uri: log://{stream}/query/summarize
  kind: query
  operation: log.summarize
  handler: python://urikv.log_handlers:log_summarize
  side_effects: false
  approval: not_required
- id: log-file-read
  uri: log://file/{path}/query/read
  kind: query
  operation: log.file.read
  handler: python://urikv.log_handlers:log_read
  side_effects: false
  approval: not_required
policy:
  default: deny_mutations_without_approval
runtime:
  default_environment: real
  supports:
  - mock
  - local
  - docker
  expose:
  - pack
  - service
  - flow
  - interface
  - adapter
```

```yaml markpact:run
scheme: log
default: flow
modes:
- pack
- service
- flow
- interface
- adapter
service:
  port: 8789
  path: /uri/call
flow:
  ids:
  - log-smoke
uses: []
adapter:
  call: POST /uri/call
  events: GET /events
```

```yaml markpact:tests
tests:
- id: log.health_query
  uri: log://runtime/query/health
  context:
    approved: true
    dry_run: true
    environment: mock
  expect:
    ok: true
    operation: log.health
```

```yaml markpact:flow id=log-smoke
flow:
  id: log-smoke
  description: Smoke log:// routes (generated from manifest).
defaults:
  approved: true
  dry_run: true
do:
- log://runtime/query/health
- log://runtime/query/streams
- log://events/query/read
```

```markdown markpact:docs
## Unpack → `.markpact/`

```bash
cd urikv
export TELLMESH_ROOT=/home/tom/github/tellmesh
PACK=markpacts/urikv-log.markpact.md
urisys markpact run "$PACK" --as flow --approve --dry-run
urisys markpact run "$PACK" --as pack
urisys markpact run "$PACK" --as service --port 8789
urisys markpact run "$PACK" --as interface
urisys markpact run "$PACK" --as adapter
```

- Source manifest: `urikv/urikv/manifest.yaml`
- Requires: `pip install -e urikv` or `TELLMESH_ROOT` for python:// handlers
```
